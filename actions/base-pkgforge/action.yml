name: Base pkgforge CI
description: Checkout repo and run make
runs:
  using: composite
  steps:
  - name: Checkout
    uses: actions/checkout@v7
    with:
      submodules: recursive
  - name: Build
    shell: bash
    run: make
